class ToDoModle {
  rowNumber: number;
  rowDis: string;
  rowAssigned: string;
  constructor(rowNumber: number, rowDis: string, rowAssigned: string) {
    this.rowNumber = rowNumber;
    this.rowDis = rowDis;
    this.rowAssigned = rowAssigned;
  }
}
